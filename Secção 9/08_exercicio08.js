const phrase = "O Davi é muito Legal e Estudioso";

const phraseArray = phrase.split(" ");

for (let index = 0; index < phraseArray.length; index++) {
  const element = phraseArray[index];

  console.log(element);
}
